# Claude 专用任务拆解版（WebMedic 项目）

## 0. 使用说明

这份文档不是总设计文档的替代品，而是它的执行版。

使用方式：

1. **先把《webmedic 开发技术细节文档》发给 Claude**，作为总约束。
2. 然后按本文件的阶段顺序，把对应阶段任务发给 Claude。
3. **一次只让 Claude 做一个阶段**，不要一次做完整个项目。
4. 每个阶段完成后，先本地运行测试，再继续下一阶段。
5. 如果 Claude 输出过多，要求它“按文件分批输出完整代码”。

---

## 1. Claude 总执行规则

以下内容建议你在第一次对话时先发给 Claude，作为统一约束。

### 发给 Claude 的总约束模板

```text
你现在要作为一个真实可落地的全栈开发助手，为 WebMedic 项目编写代码。

你必须遵守以下规则：

1. 严格遵守《webmedic 开发技术细节文档》的约束，不允许擅自改表名、接口名、目录名。
2. 你一次只完成我当前给你的这个阶段任务，不要跳到后续阶段。
3. 所有代码必须是真实可运行代码，不要只给伪代码。
4. 所有后端代码按 FastAPI + SQLAlchemy + MySQL 编写。
5. 所有前端代码按 Vue3 + Vite + Pinia + Axios 编写。
6. 输出代码时必须按文件路径分段输出。
7. 每个阶段结束时，必须补充：
   - 本阶段创建/修改了哪些文件
   - 如何启动
   - 如何测试
   - 当前已完成什么
   - 下一阶段建议做什么
8. 除非我明确要求，否则不要自行增加复杂架构。
9. 第一版优先打通最小闭环，先允许 mock，再逐步替换真实服务。
10.使用d:\code\exchangeNew相同版本的python、vue、mysql版本开发
11.拷贝D:\Code\RHIS文件夹下的deepseek_interface.py到项目中，进行deepseek调用和改进
```

---

## 2. 推荐投喂策略

### 推荐顺序

1. 先发《webmedic 开发技术细节文档》
2. 再发本任务拆解版中的“总约束模板”
3. 再发“阶段 1 任务”
4. Claude 完成后，你本地验证
5. 验证通过后，再发“阶段 2 任务”

### 不推荐的做法

* 一次性把 8 个阶段全部发给 Claude
* 要求 Claude 一次生成整个项目所有代码
* 不测试就继续下一阶段

---

## 3. 阶段总览

本项目建议拆成 8 个阶段：

1. 阶段 1：项目初始化与目录骨架
2. 阶段 2：后端基础骨架与数据库接入
3. 阶段 3：前端工作站基础页面
4. 阶段 4：会话管理与固定患者信息
5. 阶段 5：音频上传与 ASR 转写链路
6. 阶段 6：结构化抽取链路
7. 阶段 7：PDF 病历索引与相似病历检索
8. 阶段 8：病历草稿生成与临床提示引擎

最后再做：

9. 阶段 9：日志、回放、打磨与 README 完整化

---

# 阶段 1：项目初始化与目录骨架

## 1.1 目标

建立前后端目录结构、基础配置文件和可启动的空项目骨架。

## 1.2 本阶段必须完成的内容

* 创建 `D:\webmedic` 项目目录结构
* 初始化后端 FastAPI 项目骨架
* 初始化前端 Vue3 + Vite 项目骨架
* 创建 `.env.example`
* 创建基础 README
* 确保前后端都可以启动

## 1.3 本阶段不要做的事

* 不要接数据库业务表
* 不要接 ASR
* 不要接 DeepSeek
* 不要接 PageIndex
* 不要写复杂页面逻辑

## 1.4 Claude 本阶段输出文件建议

### 后端

* `backend/run.py`
* `backend/requirements.txt`
* `backend/.env.example`
* `backend/app/__init__.py`
* `backend/app/main.py`
* `backend/app/api/__init__.py`
* `backend/app/api/router.py`
* `backend/app/core/config.py`
* `backend/app/core/response.py`

### 前端

* `frontend/package.json`
* `frontend/vite.config.js`
* `frontend/src/main.js`
* `frontend/src/App.vue`
* `frontend/src/router/index.js`
* `frontend/src/views/DoctorWorkbench.vue`

### 根目录

* `README.md`

## 1.5 完成标准

* 后端访问 `/docs` 正常
* 前端页面能打开
* 页面上至少显示 “WebMedic Doctor Workbench”

## 1.6 发给 Claude 的任务模板

```text
现在开始 WebMedic 项目的第 1 阶段：项目初始化与目录骨架。

请严格按照《webmedic 开发技术细节文档》实现以下内容：

目标：
1. 建立 backend 和 frontend 的基础项目骨架
2. 后端使用 FastAPI，前端使用 Vue3 + Vite
3. 创建基础目录结构、配置文件、README、.env.example
4. 保证前后端都能启动

要求：
1. 只做第 1 阶段，不要提前实现数据库业务表、ASR、DeepSeek、PageIndex
2. 输出必须按文件路径逐个给出完整代码
3. 不要省略关键文件
4. 后端要有统一 API 路由入口
5. 前端要有基础 DoctorWorkbench 页面
6. 最后补充启动步骤和测试方法

请先输出完整项目目录树，然后分文件给出代码。
```

---

# 阶段 2：后端基础骨架与数据库接入

## 2.1 目标

让后端具备真实数据库连接能力，建立 SQLAlchemy 基础设施和核心表。

## 2.2 本阶段必须完成的内容

* MySQL 连接配置
* SQLAlchemy Base、Session 管理
* 建立核心表：

  * doctors
  * patients
  * encounter_sessions
  * transcript_segments
  * structured_records
  * medical_documents
  * similar_case_matches
  * emr_drafts
  * clinical_hints
* 初始化脚本：插入固定医生 Doctor Panython 和固定患者 张三
* 提供健康检查接口和数据库检查接口

## 2.3 本阶段不要做的事

* 不要接真实 AI 能力
* 不要做复杂业务逻辑
* 不要做 PageIndex 接入

## 2.4 Claude 本阶段输出文件建议

* `backend/app/db/base.py`
* `backend/app/db/session.py`
* `backend/app/models/*.py`
* `backend/app/api/health.py`
* `backend/app/api/system.py`
* `backend/app/scripts/init_db.py`
* 更新 `backend/app/api/router.py`

## 2.5 完成标准

* 能成功连接 MySQL
* 能创建全部基础表
* 初始化脚本执行后，数据库中有默认医生和患者数据
* 健康检查接口可访问

## 2.6 发给 Claude 的任务模板

```text
现在开始 WebMedic 项目的第 2 阶段：后端基础骨架与数据库接入。

请严格按照《webmedic 开发技术细节文档》实现：

1. MySQL 连接配置
2. SQLAlchemy 基础设施
3. 建立核心表模型
4. 编写数据库初始化脚本
5. 插入默认医生 Doctor Panython 和默认患者 张三
6. 增加健康检查接口和数据库检查接口

要求：
1. 只做第 2 阶段，不要接入 AI 服务
2. 代码按文件路径分段输出
3. 所有模型字段尽量与文档一致
4. 最后给出建库、初始化、启动、测试步骤

请先列出本阶段新增/修改文件，再逐个输出完整代码。
```

---

# 阶段 3：前端工作站基础页面

## 3.1 目标

完成前端 DoctorWorkbench 的基础布局，使其具备三栏式结构和基础交互骨架。

## 3.2 本阶段必须完成的内容

* Vue Router 配置
* Pinia 基础配置
* Axios 基础封装
* DoctorWorkbench 页面三栏布局
* 左侧患者与控制区
* 中间医患对话区
* 右侧智能辅助标签页区
* 基础样式优化

## 3.3 本阶段不要做的事

* 不要接真实录音
* 不要接真实抽取结果
* 不要接真实索引结果
* 可以先用 mock 占位

## 3.4 Claude 本阶段输出文件建议

* `frontend/src/api/http.js`
* `frontend/src/stores/app.js`
* `frontend/src/views/DoctorWorkbench.vue`
* `frontend/src/components/workbench/*.vue`
* `frontend/src/router/index.js`

## 3.5 完成标准

* 页面三栏清晰可见
* 左侧显示医生/患者信息区和按钮区
* 中间显示医生/患者对话区
* 右侧显示“结构化病历 / 相似病历 / 风险提示 / 追问建议”标签页

## 3.6 发给 Claude 的任务模板

```text
现在开始 WebMedic 项目的第 3 阶段：前端工作站基础页面。

请严格按照《webmedic 开发技术细节文档》实现：

1. Vue Router
2. Pinia
3. Axios 封装
4. DoctorWorkbench 三栏布局
5. 左侧控制区、中间对话区、右侧智能辅助区
6. 页面先用 mock 数据占位

要求：
1. 只做前端页面骨架，不接真实录音和 AI
2. 页面结构要清晰，组件可拆分就拆分
3. 输出按文件路径给出完整代码
4. 最后给出启动步骤和页面预期效果说明
```

---

# 阶段 4：会话管理与固定患者信息

## 4.1 目标

打通“开始问诊 / 结束问诊 / 加载固定医生患者信息”的前后端链路。

## 4.2 本阶段必须完成的内容

### 后端

* 创建会话接口
* 结束会话接口
* 查询默认医生和默认患者接口

### 前端

* 页面加载时获取默认医生/患者
* 点击“开始问诊”创建 session
* 点击“结束问诊”结束 session
* 页面展示当前 session 状态

## 4.3 本阶段不要做的事

* 不要接录音
* 不要接 ASR
* 不要接结构化抽取

## 4.4 Claude 本阶段输出文件建议

* `backend/app/api/session.py`
* `backend/app/api/master_data.py`
* `backend/app/services/session_service.py`
* `frontend/src/api/session.js`
* `frontend/src/api/masterData.js`
* 更新相关 store 和组件

## 4.5 完成标准

* 页面可自动显示 Doctor Panython 和张三
* 点击“开始问诊”成功创建 session
* 点击“结束问诊”成功结束 session
* 页面可显示 session_no 和状态

## 4.6 发给 Claude 的任务模板

```text
现在开始 WebMedic 项目的第 4 阶段：会话管理与固定患者信息。

请实现：
1. 查询默认医生与默认患者接口
2. 创建问诊会话接口
3. 结束问诊会话接口
4. 前端加载默认医生/患者并展示
5. 前端开始/结束问诊按钮接真实接口
6. 前端显示当前 session_no 和状态

要求：
1. 只做会话相关功能
2. 不要提前接入录音和 AI
3. 前后端代码按文件路径分段输出
4. 最后说明测试流程
```

---

# 阶段 5：音频上传与 ASR 转写链路

## 5.1 目标

完成“医生说话 / 患者说话”按钮录音上传，并返回转写结果显示到页面。

## 5.2 本阶段必须完成的内容

### 前端

* 使用 MediaRecorder 录音
* 支持“医生说话”开始/结束录音
* 支持“患者说话”开始/结束录音
* 上传音频片段到后端
* 显示返回的转写结果

### 后端

* 接收音频文件上传
* 保存音频文件到 uploads
* 建立 transcript_segments 记录
* 先做 ASR 适配层 `asr_service.py`
* 第一版可以先 mock 转写结果
* 保留未来切换 Dolphin 的封装口

## 5.3 本阶段不要做的事

* 不要做自动说话人分离
* 不要做实时 token 级流式
* 不要复杂化音频切片逻辑

## 5.4 Claude 本阶段输出文件建议

* `backend/app/api/asr.py`
* `backend/app/services/asr_service.py`
* `backend/app/services/transcript_service.py`
* `frontend/src/composables/useRecorder.js`
* `frontend/src/api/asr.js`
* 更新工作站组件

## 5.5 完成标准

* 点击“医生说话”可录音并上传
* 点击“患者说话”可录音并上传
* 页面显示转写文本
* 数据库存入 transcript_segments 记录

## 5.6 发给 Claude 的任务模板

```text
现在开始 WebMedic 项目的第 5 阶段：音频上传与 ASR 转写链路。

请实现：
1. 前端 MediaRecorder 录音能力
2. 医生/患者两种角色录音上传
3. 后端接收音频并保存文件
4. transcript_segments 落库
5. asr_service 适配层
6. 第一版先用 mock 转写结果，接口设计要能平滑切换到真实 Dolphin

要求：
1. 不要做自动说话人分离
2. 不要过度复杂化前端录音逻辑
3. 前后端代码按文件路径完整输出
4. 最后说明如何测试录音与上传链路
```

---

# 阶段 6：结构化抽取链路

## 6.1 目标

把当前会话中的医患对话抽取成结构化门诊记录。

## 6.2 本阶段必须完成的内容

### 后端

* 定义 Pydantic schema
* 封装 llm_service
* 封装 extract_service
* 提供 `/api/v1/extract/{session_id}` 接口
* 先允许 mock 输出
* 预留 Instructor + DeepSeek 的真实接入位置
* 抽取结果落库到 `structured_records`

### 前端

* 点击“重新抽取”按钮
* 请求后端抽取接口
* 右侧“结构化病历”区域显示结果

## 6.3 本阶段不要做的事

* 不要一开始就强依赖真实 API
* 不要把 prompt 写死在多个文件里

## 6.4 Claude 本阶段输出文件建议

* `backend/app/schemas/encounter_schema.py`
* `backend/app/services/llm_service.py`
* `backend/app/services/extract_service.py`
* `backend/app/prompts/extract_structured_record.txt`
* `backend/app/api/extract.py`
* `frontend/src/api/extract.js`
* 更新右侧结构化病历组件

## 6.5 完成标准

* 点击“重新抽取”能返回结构化 JSON
* 页面能展示主诉、现病史等字段
* 数据库存入 structured_records

## 6.6 发给 Claude 的任务模板

```text
现在开始 WebMedic 项目的第 6 阶段：结构化抽取链路。

请实现：
1. 定义 EncounterStructuredRecord 的 Pydantic schema
2. 封装 llm_service
3. 封装 extract_service
4. 提供 extract API
5. 抽取结果落库到 structured_records
6. 前端点击“重新抽取”后展示结构化病历结果

要求：
1. 第一版可先用 mock 输出，但代码结构必须支持后续接入 Instructor + DeepSeek
2. prompt 独立文件化
3. 代码按文件路径完整输出
4. 最后说明 mock 模式如何切换到真实模式
```

---

# 阶段 7：PDF 病历索引与相似病历检索

## 7.1 目标

支持扫描本地 PDF 病历目录，建立索引并返回相似病历结果。

## 7.2 本阶段必须完成的内容

### 后端

* 扫描本地目录接口
* 记录 medical_documents
* index_service 封装
* 第一版允许 mock 索引与 mock 检索
* 预留 PageIndex 真实接入层
* 提供 `search-similar` 接口
* 检索结果写入 `similar_case_matches`

### 前端

* 点击“检索相似病历”按钮
* 显示相似病历列表
* 展示文件名、分数、原因

## 7.3 本阶段不要做的事

* 不要在第一轮过度优化 PDF 解析
* 不要实现复杂后台任务系统

## 7.4 Claude 本阶段输出文件建议

* `backend/app/services/document_service.py`
* `backend/app/services/index_service.py`
* `backend/app/api/documents.py`
* `backend/app/api/index.py`
* `frontend/src/api/index.js`
* 右侧相似病历组件更新

## 7.5 完成标准

* 能扫描本地目录并登记 PDF 文件
* 点击检索后能返回至少 1 条相似病历 mock 结果
* 页面显示文件名、分数、原因
* 数据库存入 similar_case_matches

## 7.6 发给 Claude 的任务模板

```text
现在开始 WebMedic 项目的第 7 阶段：PDF 病历索引与相似病历检索。

请实现：
1. 扫描本地病历目录接口
2. medical_documents 入库
3. index_service 封装
4. 第一版使用 mock 索引和 mock 检索结果
5. 预留真实 PageIndex 接口层
6. search-similar API
7. similar_case_matches 落库
8. 前端展示相似病历列表

要求：
1. 不要过度复杂化 PDF 解析
2. 结构上要方便未来切换到真实 PageIndex
3. 前后端代码按文件路径完整输出
4. 最后说明如何从 mock 替换为真实索引
```

---

# 阶段 8：病历草稿生成与临床提示引擎

## 8.1 目标

完成病历草稿生成，以及风险提示、追问建议、建议检查输出。

## 8.2 本阶段必须完成的内容

### 后端

* `draft_service`
* `clinical_hint_service`
* 生成病历草稿接口
* “使用相同检查治疗方案”接口
* 生成临床提示接口
* 第一版允许 mock，预留真实 DeepSeek 接入
* `emr_drafts` 落库
* `clinical_hints` 落库

### 前端

* 点击“生成病历草稿”显示结果
* 点击“生成提示”显示风险提示/追问建议/建议检查
* 点击“使用相同检查治疗方案”可从相似病历结果中带入内容

## 8.3 本阶段不要做的事

* 不要输出确定性医疗结论
* 不要把 LLM 结果包装成正式审方系统

## 8.4 Claude 本阶段输出文件建议

* `backend/app/services/draft_service.py`
* `backend/app/services/clinical_hint_service.py`
* `backend/app/prompts/generate_emr_draft.txt`
* `backend/app/prompts/generate_clinical_hints.txt`
* `backend/app/api/draft.py`
* `backend/app/api/clinical_hints.py`
* `frontend/src/api/draft.js`
* `frontend/src/api/clinicalHints.js`
* 更新右侧相关组件

## 8.5 完成标准

* 能生成病历草稿并展示
* 能生成风险提示、追问建议、建议检查
* 能把结果落库
* 页面可点按钮触发完整流程

## 8.6 发给 Claude 的任务模板

```text
现在开始 WebMedic 项目的第 8 阶段：病历草稿生成与临床提示引擎。

请实现：
1. draft_service
2. clinical_hint_service
3. 病历草稿生成接口
4. 使用相同检查治疗方案接口
5. 临床提示生成接口
6. emr_drafts 与 clinical_hints 落库
7. 前端展示草稿、风险提示、追问建议、建议检查

要求：
1. 第一版允许 mock，但服务结构必须支持后续接入真实 DeepSeek
2. 提示语必须避免确定性医疗结论
3. 按文件路径输出完整代码
4. 最后说明如何验证最小完整闭环
```

---

# 阶段 9：日志、回放、打磨与 README 完整化

## 9.1 目标

把项目整理成可演示、可维护、可继续开发的状态。

## 9.2 本阶段必须完成的内容

* 补充统一日志
* 补充会话历史查询接口
* 补充转写记录回放页面或简单列表
* 补充文档索引状态页面
* 完善 README
* 完善 `.env.example`
* 完善错误处理

## 9.3 发给 Claude 的任务模板

```text
现在开始 WebMedic 项目的第 9 阶段：日志、回放、打磨与 README 完整化。

请实现：
1. 统一日志输出
2. 会话历史查询接口
3. 转写与结构化结果回放能力
4. 文档索引状态展示
5. 完整 README
6. 完整 .env.example
7. 完善错误处理和返回结构

要求：
1. 不改变已有接口命名
2. 只做增强和整理，不重构整个项目
3. 按文件路径完整输出代码与文档
```

---

## 4. 你每次发给 Claude 的标准末尾补充语

每次阶段任务后面，建议固定追加这一段：

```text
补充要求：
1. 先列出本阶段会新增或修改的文件清单
2. 再逐个文件输出完整代码
3. 每个文件前标注文件路径
4. 不要省略 import
5. 不要只写片段代码
6. 最后给出：
   - 启动步骤
   - 测试步骤
   - 本阶段完成说明
   - 下一阶段建议
```

---

## 5. 当 Claude 输出太长时怎么控制

如果 Claude 一次输出太多，你可以这样继续发：

### 模板 A：要求继续输出剩余文件

```text
继续，不要重复已经输出的内容。从上一次未完成的文件开始，继续按文件路径输出完整代码。
```

### 模板 B：要求先只输出后端

```text
本轮只输出后端文件，前端文件下一轮再输出。请从后端文件开始继续。
```

### 模板 C：要求先只输出一个模块

```text
本轮只输出 session 相关文件，不要输出其他模块。
```

---

## 6. 当 Claude 跑偏时怎么拉回来

如果 Claude 擅自加了复杂内容，可以发：

```text
你已经偏离当前阶段任务。请回到当前阶段范围，只实现本阶段要求，不要提前实现后续阶段内容。请基于你已经输出的代码，继续补完当前阶段缺失的文件。
```

如果 Claude 改了接口名或表名，可以发：

```text
你修改了既定接口名/表名/目录名，这不允许。请严格回到《webmedic 开发技术细节文档》中的命名，重新输出相关文件。
```

---

## 7. 最佳实际执行建议

最推荐的实操顺序是：

1. 先发总文档
2. 再发本拆解文档里的总规则
3. 再发阶段 1 模板
4. Claude 输出后你本地测试
5. 测试通过再发阶段 2
6. 依次推进到阶段 8
7. 最后做阶段 9 打磨

---

## 8. 最后的核心原则

### 原则 1

**不要让 Claude 一次生成整个项目。**

### 原则 2

**每阶段先跑通，再继续。**

### 原则 3

**允许第一版 mock，但接口和结构必须为真实接入预留位置。**

### 原则 4

**保持总文档始终作为最高约束。**

### 原则 5

**Claude 负责写代码，你负责阶段验收和纠偏。**

---

## 9. 你现在最适合直接发给 Claude 的第一条消息

```text
下面我会分两部分给你项目资料。

第一部分是《webmedic 开发技术细节文档》，它是总设计说明书，你后续所有代码都必须遵守，不允许随意改接口名、表名、目录结构。

第二部分是《Claude 专用任务拆解版》，它规定了你每一阶段具体要完成的任务。

你每次只允许完成我当前给你的阶段，不要提前开发后续阶段。
所有代码必须是真实可运行代码，按文件路径逐个输出，并在结尾给出启动和测试步骤。

现在先阅读这两份文档，接着我会给你“阶段 1：项目初始化与目录骨架”的具体执行指令。
```

---

## 10. 你下一条应该发给 Claude 的内容

先发：

* 《webmedic 开发技术细节文档》
* 本文档前面的“总执行规则”
* 阶段 1 任务模板

不要一上来全发阶段 1~9。
