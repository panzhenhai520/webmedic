##2.1目标

让后端具备真实数据库连接能力，建立 SQLAlchemy 基础设施和核心表。

## 2.2 本阶段必须完成的内容

* MySQL 连接配置
mysql本地连接的口令是www.59697.com

* SQLAlchemy Base、Session 管理
* 建立核心表：

  * doctors
  * patients
  * encounter_sessions
  * transcript_segments
  * structured_records
  * medical_documents
  * similar_case_matches
  * emr_drafts
  * clinical_hints
* 初始化脚本：插入固定医生 Doctor Panython 和固定患者 张三
* 提供健康检查接口和数据库检查接口

## 2.3 本阶段不要做的事

* 不要接真实 AI 能力
* 不要做复杂业务逻辑
* 不要做 PageIndex 接入

## 2.4 Claude 本阶段输出文件建议

* `backend/app/db/base.py`
* `backend/app/db/session.py`
* `backend/app/models/*.py`
* `backend/app/api/health.py`
* `backend/app/api/system.py`
* `backend/app/scripts/init_db.py`
* 更新 `backend/app/api/router.py`

## 2.5 完成标准

* 能成功连接 MySQL
* 能创建全部基础表
* 初始化脚本执行后，数据库中有默认医生和患者数据
* 健康检查接口可访问



现在开始 WebMedic 项目的第 2 阶段：后端基础骨架与数据库接入。

请严格按照《webmedic 开发技术细节文档》实现：

1. MySQL 连接配置
2. SQLAlchemy 基础设施
3. 建立核心表模型
4. 编写数据库初始化脚本
5. 插入默认医生 Doctor Panython 和默认患者 张三
6. 增加健康检查接口和数据库检查接口

要求：
1. 只做第 2 阶段，不要接入 AI 服务
2. 代码按文件路径分段输出
3. 所有模型字段尽量与文档一致
4. 最后给出建库、初始化、启动、测试步骤

请先列出本阶段新增/修改文件，再逐个输出完整代码。