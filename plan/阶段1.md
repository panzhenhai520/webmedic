## 1.1 目标

建立前后端目录结构、基础配置文件和可启动的空项目骨架。

## 1.2 本阶段必须完成的内容

* 创建 `D:\webmedic` 项目目录结构
* 初始化后端 FastAPI 项目骨架
* 初始化前端 Vue3 + Vite 项目骨架
* 创建 `.env.example`
* 创建基础 README
* 确保前后端都可以启动

## 1.3 本阶段不要做的事

* 不要接数据库业务表
* 不要接 ASR
* 不要接 DeepSeek
* 不要接 PageIndex
* 不要写复杂页面逻辑

## 1.4 Claude 本阶段输出文件建议

### 后端

* `backend/run.py`
* `backend/requirements.txt`
* `backend/.env.example`
* `backend/app/__init__.py`
* `backend/app/main.py`
* `backend/app/api/__init__.py`
* `backend/app/api/router.py`
* `backend/app/core/config.py`
* `backend/app/core/response.py`

### 前端

* `frontend/package.json`
* `frontend/vite.config.js`
* `frontend/src/main.js`
* `frontend/src/App.vue`
* `frontend/src/router/index.js`
* `frontend/src/views/DoctorWorkbench.vue`

### 根目录

* `README.md`

## 1.5 完成标准

* 后端访问 `/docs` 正常
* 前端页面能打开
* 页面上至少显示 “WebMedic Doctor Workbench”


现在开始 WebMedic 项目的第 1 阶段：项目初始化与目录骨架。

请严格按照《webmedic 开发技术细节文档》实现以下内容：

目标：
1. 建立 backend 和 frontend 的基础项目骨架
2. 后端使用 FastAPI，前端使用 Vue3 + Vite
3. 创建基础目录结构、配置文件、README、.env.example
4. 保证前后端都能启动

要求：
1. 只做第 1 阶段，不要提前实现数据库业务表、ASR、DeepSeek、PageIndex
2. 输出必须按文件路径逐个给出完整代码
3. 不要省略关键文件
4. 后端要有统一 API 路由入口
5. 前端要有基础 DoctorWorkbench 页面
6. 最后补充启动步骤和测试方法

请先输出完整项目目录树，然后分文件给出代码。



